<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.springboot.demo.mapper.LandMapper">
    <resultMap id="BaseResultMap" type="com.springboot.demo.bean.UserLand">
        <id column="land_id" property="landId" jdbcType="INTEGER"/>
        <result column="land_name" property="landName" jdbcType="VARCHAR"/>
        <result column="land_password" property="landPassword" jdbcType="VARCHAR"/>
    </resultMap>
    <sql id="Base_Column_List">
        land_id,
        land_name,
        land_password
    </sql>
    <select id="selectByUsername" resultMap="BaseResultMap" parameterType="com.springboot.demo.bean.UserLand">
        select
        <include refid="Base_Column_List"/>
        from land
        where land_name = #{landName,jdbcType=VARCHAR} and land_password=#{landPassword,jdbcType=VARCHAR}
    </select>

    <insert id="insert" parameterType="com.springboot.demo.bean.UserLand">
        insert into user (land_name, land_password)
        values (
        #{id,jdbcType=INTEGER},
        #{username,jdbcType=VARCHAR},
        #{password,jdbcType=VARCHAR},
        #{age,jdbcType=INTEGER})
    </insert>
    <insert id="insertSelective" parameterType="com.springboot.demo.bean.UserLand">
        insert into user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="username != null">
                username,
            </if>
            <if test="password != null">
                password,
            </if>
            <if test="age != null">
                age,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id,jdbcType=INTEGER},
            </if>
            <if test="username != null">
                #{username,jdbcType=VARCHAR},
            </if>
            <if test="password != null">
                #{password,jdbcType=VARCHAR},
            </if>
            <if test="age != null">
                #{age,jdbcType=INTEGER},
            </if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="com.springboot.demo.bean.UserLand">
        update land
        <set>
            <if test="landPassword != null">
                land_password = #{landPassword,jdbcType=VARCHAR},
            </if>
        </set>
        where land_id = #{landId,jdbcType=INTEGER}
    </update>

    <update id="updateByPrimaryKey" parameterType="com.springboot.demo.bean.UserLand">
        update user
        set username = #{username,jdbcType=VARCHAR},
        password = #{password,jdbcType=VARCHAR},
        age = #{age,jdbcType=INTEGER}
        where id = #{id,jdbcType=INTEGER}
    </update>
</mapper>