命令模式：
将一个请求封装为一个对象，从而可用不同的请求对客户进行参数化;
对请求排队或记录请求日志，以及支持可取消的操作。

实现方式：
1.首先需要一个命令接口Command，不同的命令实现命令接口执行具体的命令。
2.在具体的实现类中关联Player类（命令执行的具体内容），并在带参构造器中实例化它.
之后具体的命令执行利用关联调用Player类中的方法。
3.最后把on,off,next类关联到PlayerInvoker中。PlayerInvoker调用命令